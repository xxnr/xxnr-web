let config = require('../../../config.js')
// initial state
const state = {
  domain: config.domain,
  pcsubdomain: config.pcsubdomain
}

// mutations
const mutations = {
}

export default {
  state,
  mutations
}
